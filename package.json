{
  "name": "henryjack",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node --env-file=.env dist/server.js",
    "dev": "tsc -w & node --env-file=.env --watch dist/server.js",
    "test": "tsc && node --env-file=src/test/utils/test.env --test dist/test/*.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
